# JSON → Google Slides 変換 GASアプリ

JSONデータから高品質なGoogleスライドを自動生成するGoogle Apps Scriptアプリケーションです。
React(Vite)製の洗練されたフロントエンドUIを備え、スライドの内容だけでなくデザイン設定（カラー、フォント、フッター等）も直感的に操作してスライドを生成できます。

## 特徴

- **直感的な2カラムUI**:
  - **左カラム**: JSONエディタ。スライドの構成・内容を直接編集可能。
  - **右カラム**: デザイン設定パネル。配色、フォント、フッター、保存先などをリアルタイムに変更可能。
- **高品質な12種のスライドレイアウト**: タイトル、アジェンダ、比較図、ピラミッド図、サイクル図、プロセス図など、ビジネス用途に最適なレイアウトを網羅。
- **柔軟なカスタマイズ**: `FastAPI` + `Pydantic` 風のスキーマ設計（GAS上）により、型安全で拡張性の高い生成ロジック。
- **自動プレビュー**: スライド生成完了後、自動的に新規タブでGoogleスライドが開きます。
- **開発環境**: TypeScript + Webpack + clasp + React(Vite) のモダンなフルスタック構成。

## 実装されているスライドタイプ

| type | 内容 |
| :--- | :--- |
| `title` | タイトルスライド（日付、装飾付き） |
| `agenda` | プログラム・目次（番号バッジ付き） |
| `section` | セクション区切り（大きな番号と背景色） |
| `compare` | 左右比較（赤vs青のカラム） |
| `pyramid` | ピラミッド図（3段階の階層構造） |
| `bulletCards` | カード型箇条書き（アイコン風バッジ付き） |
| `process` | ステップ手順（矢印ナビゲーション付き） |
| `triangle` | 三角形ダイアグラム（3要素の相関関係） |
| `headerCards` | ヘッダー付きカード（2列グリッド） |
| `cycle` | サイクル図（PDCAなどの循環構造） |
| `stepUp` | 階段式ステップ（昇段形式のブロック） |
| `closing` | 締めスライド（Thank Youメッセージ） |

## 開発環境

### バックエンド (GAS)

- **TypeScript**: 型安全性とモダンな機能を利用。
- **Webpack**: モジュールを `code.js` にバンドル。
- **Clasp**: Google Apps Script CLIによるデプロイ管理。

### フロントエンド (UI)

- **React + TypeScript + Vite**: 高速な開発とモダンな構成。
- **Tailwind CSS**: 柔軟で美しいスタイリング。

## 使い方

### 1. セットアップ

1. リポジトリのルートで `npm install`
2. `frontend` ディレクトリでも `npm install`
3. `.clasp.json.example` を `.clasp.json` にコピーし、`scriptId` を設定。
4. `npx clasp login` で認証。

### 2. ローカル開発

フロントエンドの編集・確認：

```bash
cd frontend
npm run dev
```

`http://localhost:3000` でUIを確認できます（API部分はモック動作となります）。

### 3. デプロイ

バックエンドとフロントエンドをビルドし、GASにプッシュします。

```bash
# ルートディレクトリで実行
npm run build
npm run push
```

※ `npm run build` はフロントエンドの成果物をGASの `dist` にコピーする工程を含んでいます。

### 4. アプリの起動

- **GAS Webアプリ**: デプロイ後、公開したWebアプリURLにアクセスしてUIを使用します。
- **生成後の挙動**: 「プレゼンテーションを生成」ボタンをクリックすると、処理完了後に自動でGoogleスライドが新規タブで開きます。

## コマンド一覧（ルート）

| コマンド | 内容 |
|---|---|
| `npm run build` | フロントエンド・バックエンドのビルドと成果物の集約 |
| `npm run push` | GASへのデプロイ (`clasp push`) |
| `npm run watch` | 変更を監視して自動ビルド |
| `npm test` | Jestによるテスト実行 |
| `npm run open` | GASエディタを開く |
